server:
  port: 8082

spring:
  application:
    name: cards-service
  datasource:
    url: jdbc:oracle:thin:@localhost:1521:AXISDB
    username: cards_user
    password: ${ORACLE_PASSWORD:password}
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.OracleDialect

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8070/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"
